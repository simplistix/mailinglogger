language: python
python:
  - "2.7"
install:
  - pip install zc.buildout
  - buildout
script:
  - bin/nosetests --with-xunit --with-cov --cov=mailinglogger
after_success:
  - pip install coveralls
  - coveralls
