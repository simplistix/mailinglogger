language: python
python:
  - "2.7"
install:
  - pip install -r requirements-dev.txt
script:
  - nosetests --with-xunit --with-cov --cov=mailinglogger
after_success:
  - pip install coveralls
  - coveralls
